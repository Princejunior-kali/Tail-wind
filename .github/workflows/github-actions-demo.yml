name: Deployment_flow

on:
  push:
    branches: ["master"]
permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  first:
    runs-on: ubuntu-latest
    steps:
      - name: Test job
        run: echo "my first job"

      - name: Run job
        run: cat Princejunior-kali/Tail-wind/README.md

      - name: The last job
        run: echo "deployment success"

